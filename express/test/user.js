var assert = require("assert"),
User = require('../user.js');

describe('User', function(){

  /*
  describe('#equals()', function(){
    var u1 = new User('user1'),
        u2 = new User('user2'),
        u2_same = new User('user2');

    it('should equal if same name', function(){
      assert.ok(u2.equals(u2_same));
    });

    it('should not equal if defferent name', function(){
      assert.ok(!u1.equals(u2));
    });
  })
  */
})
